/**
 * MatrizLib public contract (L2/L3).
 *
 * Other applications may consume this manifest through the approved public
 * boundary. Portal internals remain private to this app.
 */
export { manifest } from "./src/manifest/manifest"
export type { MatrizLibManifest } from "./src/manifest/manifest"
