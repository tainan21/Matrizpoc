export default function DependenciesLoading() {
  return (
    <main aria-busy="true" aria-label="Carregando mapa de dependências" className="route-loading-state">
      <span>Carregando relações persistidas…</span>
    </main>
  )
}
