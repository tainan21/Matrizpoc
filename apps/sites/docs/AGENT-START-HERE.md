# Matriz Sites — Agent Start Here

Leia `AGENTS.md`, depois:

1. `src/domain/site-config.ts`;
2. `src/integration/file-site-catalog.ts`;
3. `sites/<id>/site.json`;
4. `sites/<id>/messages/<locale>.json`.

Um site é configuração e conteúdo sobre um runtime comum. Se precisar de
domínio forte, infraestrutura ou ciclo de release independente, deve ser
promovido a aplicação.
