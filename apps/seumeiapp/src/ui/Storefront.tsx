import Image from "next/image"
import Link from "next/link"
import type { ReturnTypeOfStore } from "./storefront-types"
export function Storefront({ store }: { store: ReturnTypeOfStore }) { return <main className="store-page"><header><span className="eyebrow">COMPRA SIMULADA · DEMO</span><h1>{store.displayName}</h1><p>{store.description}</p></header><section className="store-grid">{store.products.map((product) => <article key={product.variantId}>{product.imageUrl ? <Image src={product.imageUrl} alt={product.imageAlt ?? product.name} width={1200} height={900} /> : null}<div><span>{product.availabilityLabel}</span><h2>{product.name}</h2><p>{product.description}</p><footer><strong>{product.price}</strong>{product.availableQuantity > 0 ? <Link href={`/store/${store.storeSlug}/checkout?variantId=${product.variantId}`}>Comprar agora</Link> : <span>Esgotado</span>}</footer></div></article>)}</section></main> }
