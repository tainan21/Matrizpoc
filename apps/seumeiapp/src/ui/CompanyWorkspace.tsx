import type { WorkspaceViewModel } from "./presenters/company.presenter"
import { money } from "./presenters/commerce.presenter"

export function CompanyWorkspace({ workspace, operations }: { readonly workspace: WorkspaceViewModel; readonly operations: { readonly ordersToday: number; readonly pending: number; readonly averageCents: number; readonly lowStock: number } }) {
  return <main className="workspace-page workspace-overview operations-dashboard"><section className="workspace-title"><span className="eyebrow">CENTRO OPERACIONAL</span><h1>{workspace.companyName}</h1><p><span>{workspace.operationLabel}</span><span>{workspace.locationLabel}</span></p></section><section className="operations-metrics"><article><span>Pedidos hoje</span><strong>{operations.ordersToday}</strong></article><article><span>Ticket médio</span><strong>{money(operations.averageCents)}</strong></article><article><span>Em operação</span><strong>{operations.pending}</strong></article><article><span>Estoque baixo</span><strong>{operations.lowStock}</strong></article></section><section className="workspace-foundation"><div><span className="foundation-index">01</span><h2>Operação conectada.</h2></div><p>Pedidos, clientes, receitas e estoque usam dados persistidos da empresa ativa. A autoridade é revalidada no servidor em cada entrada.</p></section></main>
}
