import { listPlatforms } from "../../src/application/user-directory"
import { loadOperationalPulse } from "../../src/application/operational-pulse"
import { requireOpsPagePrincipal } from "../../src/server/ops-session"
import { AppShell } from "../../src/ui/AppShell"
export const dynamic = "force-dynamic"
export default async function PlatformsPage() { const principal = await requireOpsPagePrincipal(); if (!principal) return <div className="access-card"><h1>Acesso necessário</h1></div>; const [platforms,pulse]=await Promise.all([listPlatforms(),loadOperationalPulse()]); return <AppShell><section className="hero"><h2>Plataformas</h2><p>Registros reais do Hub e sinais das dependências operacionais.</p></section><section className="health-strip">{pulse.services.map(service=><article key={service.id}><span className={`health-dot ${service.status}`}/><div><strong>{service.label}</strong><small>{service.detail}</small></div><b className={service.status}>{service.status}</b></article>)}</section><section className="panel table-wrap"><table className="data-table"><thead><tr><th>App</th><th>Tenant</th><th>Manifest</th><th>Contrato</th><th>Status</th></tr></thead><tbody>{platforms.map(item=><tr key={item.id}><td>{item.appId}</td><td>{item.tenant.name}</td><td>{item.manifestVersion}</td><td>{item.contractVersion}</td><td><span className="pill">{item.enabled?"Ativo":"Desativado"}</span></td></tr>)}</tbody></table></section></AppShell> }
