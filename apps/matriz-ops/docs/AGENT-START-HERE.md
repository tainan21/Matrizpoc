# Matriz Ops — Agent Start Here

Matriz Ops owns internal operator policy, sanitized audit records and administrative view models. Core remains authoritative for identity, Hub for telemetry and Matriz Pay for wallet/ledger state.

Routes: `/`, `/users`, `/platforms`, `/telemetry`, `/wallets`, `/finance`, `/audit`, `/settings`.
