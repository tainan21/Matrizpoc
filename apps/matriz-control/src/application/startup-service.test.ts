import { describe, expect, it } from "vitest"
import { StartupService } from "./startup-service"
describe("startup profiles",()=>{it("preserves existing sessions and rolls back newly started members",async()=>{const started:string[]=[];const stopped:string[]=[];const service=new StartupService({projects:async()=>[{id:"matriz-hub",actions:[{id:"dev"}],port:3000},{id:"matriz-control",actions:[{id:"dev"}],port:3008}] as never,sessions:()=>[{id:"old",projectId:"matriz-hub",actionId:"dev",status:"running"}] as never,start:async(id)=>{started.push(id);throw new Error("boom")},stop:async(id)=>{stopped.push(id)},now:()=>1});const preview=await service.preview("core");await expect(service.confirm(preview.token)).rejects.toThrow("boom");expect(started).toEqual(["matriz-control"]);expect(stopped).toEqual([])})})
