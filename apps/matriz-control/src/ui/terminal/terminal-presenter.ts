import type { TerminalSession } from "../../domain/terminal"
export function terminalMetadata(session: TerminalSession, now = Date.now()) { const seconds = Math.max(0, Math.floor((now - Date.parse(session.startedAt)) / 1000)); return { route: session.route, port: session.port ? `:${session.port}` : "sem porta", duration: seconds < 60 ? `${seconds}s` : `${Math.floor(seconds / 60)}m ${seconds % 60}s`, pid: session.pid ? `pid ${session.pid}` : "sem pid", memory: session.memoryBytes == null ? "ram indisponível" : `${(session.memoryBytes / 1_000_000).toFixed(0)} mb ram`, validation: session.validationLabel ?? "não executada" } }
