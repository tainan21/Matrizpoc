/**
 * Spot — PUBLIC CONTRACT (manifest-only barrel, L2/L3).
 * Only file other apps may import from here.
 */
export { manifest } from "./src/manifest/manifest"
export type { SpotManifest } from "./src/manifest/manifest"
