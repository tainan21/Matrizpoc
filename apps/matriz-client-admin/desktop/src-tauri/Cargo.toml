[package]
name = "matriz-client-admin-desktop"
version = "0.1.0"
description = "Matriz Client Admin desktop shell"
authors = ["Matriz"]
edition = "2021"

[lib]
name = "matriz_client_admin_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
