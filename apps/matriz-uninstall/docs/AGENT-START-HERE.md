# Matriz Uninstall — Agent Start Here

Read the manifest, bootstrap, domain types and `DesktopGateway`. Distribution
behavior is app-local. The Hub is consumed through the v1 HTTP contract only.

Read `../../../docs/release-distribution.md`. Remote latest requires a live Hub stable catalog. Offline preserves inspection, uninstall, cleanup and local installers. Folder/executable paths and arguments stay in native adapters; React receives IDs and sanitized view models.
