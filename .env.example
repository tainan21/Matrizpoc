# Database URLs por app (L1) — POC V1.3 backend real.
# Estratégia: 1 Postgres físico + schemas lógicos por app via `?schema=<app>`.
# Em dev, todas URLs apontam para a mesma instância; só o schema difere.
# Extração futura (um app→repo próprio) mantém a mesma forma, mas aponta
# para instância separada — zero mudança de código.
#
# Exemplo local (Docker Compose ou Neon/Vercel Postgres shared):
#   DATABASE_URL="postgresql://user:pass@localhost:5432/matriz"
#   CORE_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=core"
#   HUB_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=hub"
#   ...

DATABASE_URL="postgresql://user:pass@localhost:5432/matriz"
CORE_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=core"
HUB_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=hub"
SPOT_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=spot"
SEUMEI_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=seumei"
CONTRACTS_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=contracts"
WILLDASH_DATABASE_URL="postgresql://user:pass@localhost:5432/matriz?schema=willdash"

# Neon central topology (Wave 2). Values are secrets supplied by the provider.
# Provisioning authority is restricted to protected infrastructure jobs.
NEON_API_KEY=
NEON_PROJECT_ID=
NEON_PRIMARY_DATABASE_URL=
# Exact Neon primary/default branch ID represented by the provisioning URL.
NEON_PROVISIONING_BRANCH_ID=
# Existing safe Postgres owner used by Neon when creating database `matriz`.
NEON_DATABASE_OWNER_NAME=
# Separate URL issued by Neon for the matriz-ci branch endpoint. Apply can stop
# after primary and resume when this secret becomes available.
NEON_CI_DATABASE_URL=

# Migration URLs are direct/unpooled and available only to migration jobs.
# Runtime URLs are pooled and scoped to each app's least-privilege role.
CORE_MIGRATION_DATABASE_URL=
CORE_RUNTIME_DATABASE_URL=
HUB_MIGRATION_DATABASE_URL=
HUB_RUNTIME_DATABASE_URL=
SPOT_MIGRATION_DATABASE_URL=
SPOT_RUNTIME_DATABASE_URL=
SEUMEI_MIGRATION_DATABASE_URL=
SEUMEI_RUNTIME_DATABASE_URL=
CONTRACTS_MIGRATION_DATABASE_URL=
CONTRACTS_RUNTIME_DATABASE_URL=
WILLDASH_MIGRATION_DATABASE_URL=
WILLDASH_RUNTIME_DATABASE_URL=
IDENTITY_ISSUER=
# Private asymmetric JWKS JSON, injected only by the Identity secret manager.
IDENTITY_SIGNING_JWKS=
IDENTITY_CSRF_SECRET=
IDENTITY_COOKIE_KEYS=
IDENTITY_TRUST_PROXY=false
# Cloud Run contract: trust exactly the final platform-added XFF hop.
IDENTITY_TRUSTED_PROXY_HOPS=0
# Absolute module URL exporting a production IdentityAuthenticator. Fail-closed.
IDENTITY_AUTHENTICATOR_MODULE=
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_DEPLOY_HOOK_MATRIZ_HUB=
VERCEL_DEPLOY_HOOK_SPOT=

# MyHub health checks: perfis adicionais allowlisted (somente HTTP/HTTPS).
# O ambiente development continua vindo do registry; nao inclua tokens nas URLs.
# MYHUB_HEALTH_PROFILES_JSON='{"preview":{"matriz-hub":"https://hub.example.com","spot":"https://spot.example.com"}}'
VERCEL_DEPLOY_HOOK_SEUMEI=
VERCEL_DEPLOY_HOOK_CONTRACTS=
VERCEL_DEPLOY_HOOK_WILLDASH=
SPLIT_REPO_PAT=

# Matriz Workbench — local-only. Use a unique value with at least 16 chars.
WORKBENCH_LOCAL_TOKEN=
# Optional override when the process cannot infer the monorepo root.
MATRIZ_REPO_ROOT=
