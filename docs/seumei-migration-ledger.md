# Seumei migration ledger

## Permanent ownership and current proof

- `apps/seumeiapp` owns company, onboarding, workspace and every future Seumei business capability.
- `prisma/schemas/core.prisma` owns global user, tenant, app registration and membership.
- `prisma/schemas/seumei.prisma` owns company, onboarding, catalog, recipes, ingredient stock, publication, customers and commerce persistence.
- `apps/matriz-admin` remains an external observer through public contracts; it does not own or query Seumei data directly.
- The assimilated foundation creates a real tenant/company/OWNER membership, resumes persisted onboarding, enters a membership-authorized workspace and manages an app-scoped team. The browser never supplies tenant authority.

The read-only reference is `apps/incoming/seumei-reference` in the main checkout. Paths below are evidence locations, not runtime dependencies.

## Reference map

- Workspace and multi-company: `features/space/**`, `features/space-runtime/**` and `app/w/[slug]/**`.
- Onboarding and settings: `features/space-onboarding/**` and `features/space-settings/**`.
- Catalog and stock: `modules/product/**` and `modules/stock/**`.
- Store and publication: `modules/store/**`, `features/store-runtime/**` and `app/loja/[slug]/**`.
- Commerce: `modules/orders/**`, `modules/customers/**` and `modules/finance/**`.
- Visual configuration: `features/design/**`, `modules/design/**` and `config/store-*.ts`.
- Reference persistence is predominantly local/mock (`local-*-repository.ts`, `mock-*-repository.ts`, browser storage). Its rules are evidence; its persistence architecture is not preserved.

## Assimilation ledger

| Capacidade | Comportamento observado | Fonte de evidência | Dados e dependências | Classificação / prioridade | Destino arquitetural | Estratégia de teste | Estado |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Ciclo de empresa/space | Criar, listar, selecionar e abrir um workspace por slug | `features/space/application/space-service.ts`; `features/space/presentation/space-switcher.tsx` | Empresa; Core tenant; membership; Seumei app registration | **RECONSTRUIR / P0** — a jornada tem valor, mas a autoridade local/mock não | `src/application/provision-company.ts`, `company-access.ts`, repositories Core/Seumei | Idempotência, compensação, slug duplicado, lista autorizada e componentes | **ASSIMILADO** |
| Tenant e OWNER inicial | O criador inicia com controle do workspace | `features/space-settings/presentation/space-members-settings-section.tsx` | Core `User`, `Tenant`, `Membership`, `AppRegistration` | **PRESERVAR CONTRATO / P0** — ownership é invariante de interoperabilidade | `src/infrastructure/core-access.repository.ts` | Transação Core e membership `OWNER` app-scoped | **ASSIMILADO** |
| Seleção de empresa ativa | Slug/space ativo persiste no cliente/cookie e controla navegação | `features/space/infrastructure/space-storage.ts`; `server-space-resolver.ts` | Company ID como preferência; memberships persistidas como autoridade | **SUBSTITUIR / P0** — storage não pode conceder acesso | Cookie HTTP-only + `src/application/active-company.ts` | Cookie adulterado e ID conhecido de outro tenant retornam negação genérica | **ASSIMILADO** |
| Evento público de seleção | Consumidores Matriz observam `seumei.establishment.selected` | Contrato canônico `packages/integration/events/src/index.ts`; consumo Hub/Contracts | Company validada mapeada para o payload legado após autorização | **PRESERVAR CONTRATO / P0** — interoperabilidade existente exige compatibilidade | Emissão server-side em `src/application/composition.ts`; nenhum dado vai ao browser | Handler prova emissão somente após seleção autorizada; smoke de manifests/eventos | **ASSIMILADO COM ADAPTER DE COMPATIBILIDADE** |
| Onboarding retomável | Draft por etapas de identidade/configuração continua após navegação | `features/space-onboarding/domain/space-onboarding-draft.ts`; `local-space-onboarding-repository.ts` | `CompanyOnboarding`, versão otimista, preferência de moeda | **ADAPTAR / P0** — preservar jornada, trocar persistência | `src/application/company-onboarding.ts`; schema e repository Seumei | Retomada, versão obsoleta, campos obrigatórios, conclusão idempotente | **ASSIMILADO** |
| Autorização tenant | Gate de rota associa usuário ao space, mas convive com fontes locais | `features/space-runtime/application/space-route-access.ts`; `space-route-gate.tsx` | Sessão Hub + Core user/membership + company tenant-scoped | **CORRIGIR / P0** — autoridade deve ser server-side por construção | `company-access.ts`, `server-page-context.ts`, handlers HTTP | Dois tenants com IDs conhecidos; repositories nunca têm lookup empresarial sem tenant | **ASSIMILADO** |
| Estados honestos | Loading/not-found aparecem, mas mocks podem mascarar ausência | `app/w/[slug]/not-found.tsx`; `features/space/infrastructure/mock-space-catalog.ts` | Sessão e configuração de banco | **SUBSTITUIR / P0** — ausência não vira dado falso | `database-config.ts`, `SystemState.tsx`, `loading.tsx` | Signed-out vs indisponível; config ausente sem construir Prisma; UI vazia | **ASSIMILADO** |
| Shell, membros e permissões | Diretório, matriz de ações e gates de rota são úteis; role switcher, identidades e memberships são seeds em `localStorage` | `features/permissions/**`; `space-members-settings-section.tsx`; `space-route-access.ts` | Core `Membership` e convite app-scoped com token hasheado; política Seumei OWNER/ADMIN/MEMBER/VIEWER | **ADAPTAR / P1** para jornada e decisões; **SUBSTITUIR / P0** para autoridade local; **NÃO ASSIMILAR / P3** para role switcher mock | Convite/membership no Core; capacidades, serviços, presenters e shell app-local; nenhum package novo | Matriz completa, token expirado/revogado/e-mail divergente, owner protegido, IDs conhecidos entre tenants A/B e browser desktop/mobile | **ASSIMILADO — SPEC `2026-08-20-seumei-memberships-permissions-design.md`** |
| Produtos, variantes e categorias | CRUD workspace-scoped, slugs, estados e produtos simples/configuráveis; preço float, opção em JSON, estoque obrigatório e repository local são dívida | `modules/product/domain/**`; `modules/product/application/product-service.ts`; `types/domain.ts` | Produto, categoria, variante relacional, preço em centavos, company/tenant | **ADAPTAR / P1** para comportamento; **SUBSTITUIR / P0** para storage/autoridade; **CORRIGIR / P1** para dinheiro, variantes e acoplamento ao estoque | Domínio, repository, serviços, presenters e UI app-local; schema Seumei; tenant vindo da sessão/membership | Invariantes de variante, conflito slug/SKU, versão otimista, CRUD com IDs conhecidos entre tenants A/B, roles e browser desktop/mobile | **ASSIMILADO — SPEC `2026-08-22-seumei-catalog-route-flows-design.md`** |
| Laboratório de route flows | Necessidade de acompanhar rotas implementadas e rascunhar sequências durante a assimilação | Direção de produto de 2026-08-22; rotas canônicas de company/onboarding/members/catalog | Definições versionadas app-local; rascunho temporário em memória; nenhuma autoridade ou persistência empresarial | **RECONSTRUIR / P1** — ferramenta temporária e removível, não domínio | `/docs`, parser app-local e link temporário no shell | Parser por linha, rotas relativas, preview/export Markdown, autenticação, desktop/mobile e remoção sem migration | **ASSIMILADO — TEMPORÁRIO ATÉ LANÇAMENTO** |
| Estoque e movimentos | Saldo, threshold, saúde e timeline; a referência separa saldo/histórico no browser, mas restaurante exige consumo por receita | `modules/stock/**`; evidência negativa em `modules/product/**` para ausência de receitas estruturadas | Ingrediente, receita, saldo/versionamento, movimento, ator e idempotência | **RECONSTRUIR / P1** — preservar intenção operacional, corrigir o agregado para ingrediente e substituir storage local | `IngredientInventory` + `IngredientStockMovement` app-local, Prisma Seumei e APIs tenant-scoped | Regras de unidade/receita, atomicidade, concorrência, idempotência e isolamento tenant A/B; browser desktop/mobile | **ASSIMILADO — unidades inteiras, receitas versionadas, movimentos append-only e baixa por pedido** |
| Loja e publicação | Configuração e páginas públicas por slug; referência demonstra fluxo, mas usa persistência local/mock | `modules/store/**`; `features/store-runtime/**`; `app/loja/[slug]/**` | Publicação, routing, catálogo público e versão | **RECONSTRUIR / P1** — routing público exige contrato próprio e resolução server-side | Domínio Seumei + `/store/[storeSlug]` e API pública versionada | Publicado, isolamento, cache seguro, slug server-side e browser | **ASSIMILADO PARA DEMO — publicação persistente; editor/draft avançado permanece P2** |
| Pedidos | Snapshots de item/cliente, timeline e operação orientada ao workspace; storage local e integrações simuladas são incompatíveis | `modules/orders/**`; `features/order-automation/**` | Pedido, itens, status, cliente, receita e estoque | **ADAPTAR / P1** para lifecycle/snapshots; **SUBSTITUIR / P0** para persistência/efeitos | Transação serializável Seumei com consumo de ingredientes e timeline imutável | Máquina de estados, idempotência, rollback, IDs conhecidos tenant A/B e browser | **ASSIMILADO PARA COMPRA SIMULADA — pagamento/fiscal não assimilados** |
| Clientes | Cadastro/consolidação usa e-mail, documento e telefone, mas deriva clientes de pedidos e contém fallbacks frágeis | `modules/customers/**` | Cliente tenant-local, contatos normalizados e pedidos | **CORRIGIR / P1** — preservar identidade comercial, eliminar derivação/browser e heurística ambígua | `Customer` persistido no Seumei; nunca promove automaticamente a Core user | E-mail/telefone normalizados, rollback de checkout e isolamento | **ASSIMILADO PARA IDENTIDADE POR E-MAIL; merge/documento/consentimento permanecem P2** |
| Financeiro essencial | Entradas/saídas, previsão/reconhecimento e consolidação mensal; a referência deriva pedidos no browser, usa float, permite remoção e estima CMV sem custo canônico | `modules/finance/**`; `finance-service.ts`; `local-finance-repository.ts` | Lançamentos em centavos, eventos, competência/vencimento/pagamento e pedidos persistidos | **RECONSTRUIR / P1** — preservar a leitura operacional, substituir storage/float/remoção e não inventar CMV | Bounded context app-local; recebimento de pedido persistido atomicamente e lançamentos manuais auditáveis; OWNER/ADMIN somente | Invariantes monetárias/datas, idempotência do pedido, transições, versão otimista, dois tenants com IDs conhecidos e browser desktop/mobile | **ASSIMILADO — `/workspace/finance/**`, migration `202608240003_essential_finance`, aceitação `2026-08-24-seumei-essential-finance-acceptance.md`** |
| Identidade visual da loja | Editor local-first extenso, presets, tokens livres e registry de blocos; preview isolado é valioso, mas browser storage e CSS arbitrário não servem como publicação | `features/design/**`; `modules/design/**`; `config/store-theme-registry.ts`; `docs/theme-editor-final-pass.md` | Draft tenant-scoped, preset validado, conteúdo hero, versão publicada imutável e estado público | **ADAPTAR / P1** — preservar catálogo/preview/aplicação explícita, substituir storage e reduzir o editor livre | Bounded context app-local sobre `StorePublication`; presets acessíveis em código, draft persistido, snapshot de publicação e storefront por presenter | Validação de preset/conteúdo, versão otimista, draft invisível, publicar/despublicar, dois tenants, contraste/foco e browser desktop/mobile | **EM ASSIMILAÇÃO — próxima fatia após financeiro** |
| Primitivos UI duplicados | A referência contém grande árvore `components/ui/**` | `components/ui/**` | Componentes visuais genéricos | **ELIMINAR DUPLICAÇÃO / P2** — MatrizLib já é canônica | Consumir `@matriz/design-ui` / `@matriz/design-system` | Component tests e acessibilidade no consumidor | **EM ANDAMENTO POR FATIA** |
| Repositories local/mock | Dados de produto, estoque, loja, pedido e finanças no browser | `modules/**/infrastructure/local-*`; `mock-*` | `localStorage`, mocks e fallback | **SUBSTITUIR / P0–P2** — não é persistência de produto | Prisma/repositories server-side somente quando a fatia existir | Refresh, reinício e negação cross-tenant | **SUBSTITUÍDO EM EMPRESA, CATÁLOGO, RECEITAS, ESTOQUE, LOJA DEMO, PEDIDOS E CLIENTES; FINANCEIRO ADIADO** |
| Analytics/demo/layout experimental | Dashboards, telemetria de demo e variantes amplas sem fluxo estável | `modules/analytics/**`; `features/commerce-telemetry/**`; `features/layout-variants/**` | Dados demonstrativos e preferências locais | **NÃO ASSIMILAR / P3** — não sustenta a jornada prioritária | Nenhum destino atual | Reavaliar apenas com caso de uso e dados reais | **REJEITADO NESTA MIGRAÇÃO** |

## Próxima fatia recomendada

Assimilar identidade visual/publicação como próxima fatia: draft versionado, presets/tokens limitados, preview privado e publicação explícita. Page builder, domínio customizado e analytics permanecem fora até evidência real.
