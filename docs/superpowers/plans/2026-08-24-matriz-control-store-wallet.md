# Store and Wallet implementation plan

1. Add native commerce tests for ledger invariants, ownership/install separation and atomic persistence.
2. Implement bundled catalog and native `CommerceStore`.
3. Expose exact typed Tauri commands and activity events.
4. Add Store navigation, prominent Wallet, catalog states and transaction history.
5. Connect installed packages to existing runtime actions.
6. Validate native/frontend tests, visual behavior and Windows packaging.
