# @matriz/platform-storage

## Responsibility (L9)
In-memory + localStorage storage primitives used by mock repositories (L5),
plus the small HTTP client used by the local cross-app cache proof.

## May import `@matriz/foundation-*`. Must NOT import apps.
