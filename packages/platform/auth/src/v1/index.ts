/**
 * `@matriz/platform-auth/v1` — versioned public surface (L7).
 *
 * Server-safe v1 public surface. React hooks/provider/context live under
 * `@matriz/platform-auth/client`.
 */
export * from "../server"
