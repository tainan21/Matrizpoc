# @matriz/flows-auth

Shared passwordless login composition for the five authenticated Matriz apps.
It coordinates technical auth contracts and visual primitives without owning
any product domain. App-specific identity is supplied through `LoginSkin` and
optional React slots.
