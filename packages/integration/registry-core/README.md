# @matriz/integration-registry-core

## Responsibility (L9)
Registry types + `Registry` class + lookup helpers. Apps register their own
manifests here (L2). This package holds NO authoritative manifests.

## Must NOT import apps (L4) or design packages (L4).
