# @matriz/foundation-constants

## Responsibility (L9)
Cross-monorepo constants and enums (app IDs, tenant IDs, contract version).

## Exposes
- `MATRIZ_APP_IDS`, `MatrizAppId`, `CONTRACT_VERSION_V1`, `MATRIZ_MOCK_TENANT_IDS`.

## Must NOT import anything else (L4 — foundation is the base layer).
