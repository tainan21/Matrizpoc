# @matriz/foundation-schemas

## Responsibility (L9)
Base Zod schemas shared by all packages (ISO date, tenant/app id).

## Exposes
- `isoDateStringSchema`, `tenantIdSchema`, `appIdSchema`, re-exported `z`.

## Must NOT import other @matriz/* packages (L4).
